@using WebEditor.ViewComponents;

@{
    ViewData["Title"] = "Home Page";
}
@{
    //Layout=null;
}
<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width">
        <title>@ViewData["Title"]</title>
    </head>
    <body>
        <div>
            @*Html.Partial("")*@
        </div>
        <div>
            <span>in here</span>
            @await Component.InvokeAsync(nameof(ProductList))
        </div>
        <div>
            @*await Html.PartialAsync("Error1")*@
        </div>
    </body>
</html>
