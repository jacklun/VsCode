using System.Collections;
using System.Collections.Generic;
using System.ComponentModel;
using WebEditor.Models;

namespace WebEditor.ViewModels {
    public class ProductVM {
        /*
        public int id { get; set; }

        public int tid { get; set; }

        public string pname { get; set; }

        public string description { get; set; }

        public decimal price { get; set; }

        public decimal discount { get; set; }

        public string image_url { get; set; }
        */
        /*
        Product _product;
        public Product product {
            get {
                return _product;
            }
            set {
                _product = value;
                _propertys.Add("id", product.id);
                _propertys.Add("tid", product.id);
                _propertys.Add("pname", product.pname);
                _propertys.Add("description", product.description);
                _propertys.Add("price", product.price);
                _propertys.Add("discount", product.discount);
                _propertys.Add("image_url", product.image_url);
            }
        }

        Hashtable _propertys = new Hashtable ();
        public Hashtable Propertys {
            get {
                return _propertys;
            }
        }
        */
    }
}